<h1>Articles</h1>

<h2> Our blog has <%= Article.public_count %> posts and counting!</h2>
<ul>
  <% @articles.each do |article| %>
    <% unless article.archived? %>
      <li>
        <%= link_to article.title, article %>
      </li>
      <p>
        <%= article.body %>
      </p>
    <% end %>
  <% end %>
</ul>

<%= link_to "Add New Article", new_article_path %>