
<%= form_with model: [ article, article.comments.build] do |form| %>

    <div>
        <%= form.label :commenter %> <br>
        <%= form.text_field :commenter %>
    </div><br>

    <div>
        <%= form.label :body %> <br>
        <%= form.text_area :body %>
    </div><br>

    <div>
        <%= form.label :status %> <br>
        <%= form.select :status, Visible::VALID_STATUSES, selected: 'public' %> <%#i didn't do a article.comment.status here bc you can't edit a comment, so there would be no preexisting status to render other than public %>
    </div><br>

    <div>
        <%= form.submit %>
    </div>

<% end %> 